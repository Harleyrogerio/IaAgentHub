# Dockerfile para Frontend Estático - Hartech Portfolio
FROM node:20-alpine

# Instalar serve globalmente
RUN npm install -g serve

# Definir diretório de trabalho
WORKDIR /app

# Copiar package files
COPY package*.json ./

# Instalar dependências (incluindo vite para build)
RUN npm ci

# Copiar código fonte
COPY . .

# Build do projeto (gera pasta dist)
RUN npm run build

# Expor porta 5000
EXPOSE 5000

# Servir arquivos estáticos da pasta dist
CMD ["serve", "-s", "dist", "-l", "5000"]